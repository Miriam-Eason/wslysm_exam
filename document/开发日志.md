# 开发日志

> **定位**：每步完成后追加一条记录，作为决策历史和踩坑记录库。
> **当前进度概览** → 看 `/CLAUDE.md`（Claude Code 自动加载，始终最新）。
> **API/页面/进程表** → 看 `document/开发PRD文档.md`。

---

## 追加记录模板

复制以下模板，填入内容后追加到本文件末尾：

```
## [YYYY-MM-DD] Sxx — 步骤名称 ✅

### 完成内容
-

### 遇到的问题及解决方案
- 问题：
  解决：

### 尚未解决 / 暂缓（更新到 CLAUDE.md 已知问题）
-

### 本步骤新产生的决策
-

### 下一步
进入 Sxx+1（具体任务：...）
```

---

## [2026-06-29] 项目设计阶段完成 ✅

### 完成内容
- 完成全部 6 份设计文档（数据模型总表 / 开发PRD / 学生导入 / 题库导入 / 题目管理架构 / 技术路线）
- 解决文档间 7 个阻断级冲突（主键统一 / 身份建模 / Question 合并 / 题型统一 / 难度枚举 / JSON 格式 / 去重键）
- 所有架构决策全部拍板（见 CLAUDE.md 已拍板决策表）
- 建立双文件进度追踪系统（CLAUDE.md + 开发日志.md）

### 关键决策（本阶段全部拍板）
- 主键：全库 Int 自增
- 身份：独立 Student + Teacher 表；Admin = Teacher.isAdmin
- 题型：4种（单选/多选/判断/填空），简答=二期
- 填空判分：归一化后全空命中才算对
- 去重键：`@@unique([bankId, contentHash])` 库内唯一
- Nginx 正式纳入 docker-compose
- 草稿保存：切题立即 + 每 15s 防抖

### 下一步
进入 S0：创建 Next.js 项目，写 schema.prisma，跑通 migrate + seed

---

## [2026-06-29] S0 — 脚手架 + Schema + Seed ✅

### 完成内容
- `create-next-app` 创建 Next.js **16.2.9**（App Router + TS + Tailwind v4 + src 目录 + ESLint）
- 依赖：`@prisma/client` `bcryptjs`（运行）；`prisma` `tsx` `@types/bcryptjs`（开发）
- **Prisma 锁定 6.19.3**（npm 默认装了 7.8，主动降级——见决策）
- 创建本机 MySQL 数据库 `exam_system`（utf8mb4_unicode_ci）
- 写 `prisma/schema.prisma`：11 张表 + 4 枚举，严格按《数据模型总表》
- `prisma migrate dev --name init` 建表成功（迁移 `20260629073933_init`）
- `src/lib/prisma.ts` PrismaClient 单例（挂 globalThis）
- `.env` / `.env.example`（DATABASE_URL + NEXTAUTH 占位）
- `.gitignore` 增强：保留 `.env.example`、忽略 `/backup` `*.sql` `/nginx/certs` 等
- `prisma/seed.ts` + package.json `prisma.seed` 配置 + `db:seed`/`db:reset` 脚本
- `next.config.ts`：`output: standalone`（Docker 需要）+ 固定 turbopack root

### 验收结果（全部通过）
- 各表记录数核对：Teacher 3 / Student 30 / Class 2 / Enrollment 30 / QuestionBank 2 / Question 5 / Exam 1 / ExamQuestion 5 / ExamClass 1 / Attempt 3 / AnswerItem 15 / WrongQuestion 3 ✓
- seed 三名学生得分 **100 / 80 / 60**，与手算一致 → 间接验证判分逻辑（选择类集合相等 + 填空全空命中）✓
- `tsc --noEmit` 类型检查零错误 ✓
- `npm run dev` 启动正常，首页 HTTP 200，workspace root 警告已消除 ✓

### 本步骤新产生的决策
- **Prisma 6 而非 7**：7 改了客户端生成器（prisma-client，ESM-first）和默认输出路径，会与技术路线 Dockerfile 的 `node_modules/.prisma` 拷贝步骤冲突，对初学项目不友好。后续 `prisma` CLI 升级提示一律忽略。
- seed 内置了与正式判分一致的 `grade()` / `contentHash()` 辅助函数，S5/S9 实现时应抽到 `src/lib/` 复用，保持口径统一。

### 尚未解决 / 暂缓
- `~/package-lock.json`（用户主目录游离 lockfile）已用 turbopack.root 规避，未删除。
- 开发账号密码（admin123/teacher123）为明文便于联调，部署前需更换。

### 下一步
进入 **SD**（Stitch 前端设计）：出 4 个关键页面设计稿 → 提取 DESIGN.md → token 落地到 Tailwind 配置。

---

_（后续记录追加在此行之后）_
