# 开发日志

> **定位**：每步完成后追加一条记录，作为决策历史和踩坑记录库。
> **当前进度概览** → 看 `/CLAUDE.md`（Claude Code 自动加载，始终最新）。
> **API/页面/进程表** → 看 `document/开发PRD文档.md`。

---

## 追加记录模板

复制以下模板，填入内容后追加到本文件末尾：

```
## [YYYY-MM-DD] Sxx — 步骤名称 ✅

### 完成内容
-

### 遇到的问题及解决方案
- 问题：
  解决：

### 尚未解决 / 暂缓（更新到 CLAUDE.md 已知问题）
-

### 本步骤新产生的决策
-

### 下一步
进入 Sxx+1（具体任务：...）
```

---

## [2026-06-29] 项目设计阶段完成 ✅

### 完成内容
- 完成全部 6 份设计文档（数据模型总表 / 开发PRD / 学生导入 / 题库导入 / 题目管理架构 / 技术路线）
- 解决文档间 7 个阻断级冲突（主键统一 / 身份建模 / Question 合并 / 题型统一 / 难度枚举 / JSON 格式 / 去重键）
- 所有架构决策全部拍板（见 CLAUDE.md 已拍板决策表）
- 建立双文件进度追踪系统（CLAUDE.md + 开发日志.md）

### 关键决策（本阶段全部拍板）
- 主键：全库 Int 自增
- 身份：独立 Student + Teacher 表；Admin = Teacher.isAdmin
- 题型：4种（单选/多选/判断/填空），简答=二期
- 填空判分：归一化后全空命中才算对
- 去重键：`@@unique([bankId, contentHash])` 库内唯一
- Nginx 正式纳入 docker-compose
- 草稿保存：切题立即 + 每 15s 防抖

### 下一步
进入 S0：创建 Next.js 项目，写 schema.prisma，跑通 migrate + seed

---

## [2026-06-29] S0 — 脚手架 + Schema + Seed ✅

### 完成内容
- `create-next-app` 创建 Next.js **16.2.9**（App Router + TS + Tailwind v4 + src 目录 + ESLint）
- 依赖：`@prisma/client` `bcryptjs`（运行）；`prisma` `tsx` `@types/bcryptjs`（开发）
- **Prisma 锁定 6.19.3**（npm 默认装了 7.8，主动降级——见决策）
- 创建本机 MySQL 数据库 `exam_system`（utf8mb4_unicode_ci）
- 写 `prisma/schema.prisma`：11 张表 + 4 枚举，严格按《数据模型总表》
- `prisma migrate dev --name init` 建表成功（迁移 `20260629073933_init`）
- `src/lib/prisma.ts` PrismaClient 单例（挂 globalThis）
- `.env` / `.env.example`（DATABASE_URL + NEXTAUTH 占位）
- `.gitignore` 增强：保留 `.env.example`、忽略 `/backup` `*.sql` `/nginx/certs` 等
- `prisma/seed.ts` + package.json `prisma.seed` 配置 + `db:seed`/`db:reset` 脚本
- `next.config.ts`：`output: standalone`（Docker 需要）+ 固定 turbopack root

### 验收结果（全部通过）
- 各表记录数核对：Teacher 3 / Student 30 / Class 2 / Enrollment 30 / QuestionBank 2 / Question 5 / Exam 1 / ExamQuestion 5 / ExamClass 1 / Attempt 3 / AnswerItem 15 / WrongQuestion 3 ✓
- seed 三名学生得分 **100 / 80 / 60**，与手算一致 → 间接验证判分逻辑（选择类集合相等 + 填空全空命中）✓
- `tsc --noEmit` 类型检查零错误 ✓
- `npm run dev` 启动正常，首页 HTTP 200，workspace root 警告已消除 ✓

### 本步骤新产生的决策
- **Prisma 6 而非 7**：7 改了客户端生成器（prisma-client，ESM-first）和默认输出路径，会与技术路线 Dockerfile 的 `node_modules/.prisma` 拷贝步骤冲突，对初学项目不友好。后续 `prisma` CLI 升级提示一律忽略。
- seed 内置了与正式判分一致的 `grade()` / `contentHash()` 辅助函数，S5/S9 实现时应抽到 `src/lib/` 复用，保持口径统一。

### 尚未解决 / 暂缓
- `~/package-lock.json`（用户主目录游离 lockfile）已用 turbopack.root 规避，未删除。
- 开发账号密码（admin123/teacher123）为明文便于联调，部署前需更换。

### 下一步
进入 **SD**（Stitch 前端设计）：出 4 个关键页面设计稿 → 提取 DESIGN.md → token 落地到 Tailwind 配置。

---

## [2026-06-30] SD — 前端设计（改为手写 DESIGN）✅

### 完成内容
- 评估后**放弃用 Stitch 出稿**（静态图转译损耗高、ROI 低；shadcn/ui 已具备 Apple 基础观感）
- 手写 `design/学生端风格design.md`（Serene Study 设计系统）：颜色 / 字体（Inter）/ 圆角 / 间距 / 毛玻璃 token + 各组件规格
- 补齐关键缺口：主色统一 `#007AFF`（iOS 蓝）、语义色（success/warning/danger）、底部 TabBar、做题页（Header/计时器/选项卡四态/答题卡抽屉/交卷确认）、结果页、加载骨架、空状态、教师/超管端（Sidebar 240px + 表格/弹窗约定）
- 三个待定项已拍板：选项选中态=填色背景；题目导航=顶部进度条+底部答题卡抽屉（数字圆点）；教师端=左侧 Sidebar

### 本步骤新产生的决策
- **不引入 tailwind.config.ts**：项目是 Tailwind v4，token 一律写进 `globals.css` 的 `@theme`（`--color-*` / `--radius-*`），`@theme inline` 绑定 next/font 的 Inter 变量

### 下一步
进入 S1（鉴权系统）

---

## [2026-06-30] S1 — 鉴权系统 ✅

### 完成内容
- **NextAuth 选型 v5（Auth.js，`next-auth@5.0.0-beta.31`）**：v4(4.24.14) 不支持 Next16/React19；v5 peerDeps 明确 `next ^16` + `react ^19`
- **split config 模式**：`src/auth.config.ts`（Edge 安全，仅 jwt/session 回调，不引入 Prisma）+ `src/auth.ts`（注入访问数据库的 Credentials Provider）
- `authorize` 双身份判分：学生查 `Student.studentNo`（过滤 `deletedAt`）；教师/超管查 `Teacher.username`；**admin 门户强制校验 `isAdmin`**；role 取自登录门户
- `src/proxy.ts` 路由隔离（Next16 已将 `middleware.ts` 改名为 `proxy.ts`）：未登录→本区登录页(带 callbackUrl)；跨角色→各自首页；已登录访登录页→回首页
- **服务端二次鉴权** `src/lib/auth-guard.ts` 的 `requireRole()`：满足 PRD §line113「middleware + handler 双重校验」，三个受保护页均接入
- 三套登录页（教师/超管 PC 卡片 / 学生移动毛玻璃）共用 `LoginForm`（Zod 校验、`signIn(redirect:false)`、安全 callbackUrl 回跳）
- 设计 token 落地 `globals.css`（Tailwind v4 `@theme`）+ `layout.tsx` 换 Inter 字体 + `SessionProvider`
- 安装 `zod`；类型增强 `src/types/next-auth.d.ts`

### 验收结果（全部通过）
- `tsc --noEmit` 0 错误；`next build` 0 警告（proxy 正确编译为 Edge）
- **18 项 curl 集成测试全过**：三角色登录成功 / 错误凭证被拒 / 普通教师走 admin 门户被拒 / 未登录跳本区登录 / 跨角色拦截回首页 / 同角色 200 放行 / 已登录(含异区)访登录页回首页
- 浏览器截图确认学生(移动)与教师(PC)登录页 Apple 观感渲染正确

### 遇到的问题及解决方案
- 问题：`declare module "next-auth/jwt"` 的 JWT 类型增强不生效（token 字段为 unknown）
  解决：`next-auth/jwt` 只是 `export * from "@auth/core/jwt"` 的纯再导出，须直接增强 `@auth/core/jwt`
- 问题：`next build` 报 `middleware` 约定已废弃
  解决：按 Next16 改名为 `src/proxy.ts`（功能等价，警告消除）
- 问题：`next start` 与 `output:standalone` 不完全兼容（仅自测期）
  解决：自测可用；正式部署按技术路线用 `node .next/standalone/server.js`

### 代码评审（多智能体对抗式审查，11 项 raise → 4 项确认并已修复）
- [medium] 受保护页缺服务端鉴权（单点依赖 proxy）→ 加 `requireRole()` 二次校验
- [low] callbackUrl 用 `startsWith` 无路径段边界 → 改 `=== homePath || startsWith(homePath+"/")`
- [low] 已登录用户访问异区登录页未重定向 → 登录页分支改为「任何已登录用户回各自首页」
- [low] 登录按钮 52px < design 规定 56px → 改 `h-14`，删冗余 inline style
- 已驳回 7 项（如 .env.example「v4 变量名」属误判：v5 实际兼容读取 NEXTAUTH_SECRET；时序侧信道枚举留待加限流时一并处理）

### 尚未解决 / 暂缓
- 登录无频率限制 + authorize 存在账号枚举时序侧信道 → 留待后续登录加固（配合限流）一并处理
- 开发账号明文密码（admin123/teacher123）部署前需更换；`NEXTAUTH_SECRET` 生产需换随机值

### 下一步
进入 **S2**（班级管理）：班级 CRUD API + UI、班内学生列表分页、Excel 模板下载。

---

## [2026-06-30] S1 补 — 登录页校徽与系统名 ✅

### 背景
S1 功能验收无误后，按需求统一品牌：三端登录标题改为「无锡旅商智能练测系统」并加入学校校徽。

### 完成内容
- 校徽资产：原文件 `design/校标+无锡旅商（不带印章定稿）.png`（1500×234）复制为 **`public/school-logo.png`**（原名含中文+括号，URL 会乱码，故改干净 ASCII 名）
- 新建共享组件 **`src/components/auth/login-brand.tsx`**（`LoginBrand`）：校徽（`next/image` + `priority`，按原比例约束宽度）+ 发丝分隔线 + 门户标签 + 系统名「无锡旅商智能练测系统」+ 副标题；`align`(left/center) 与 `roleClassName` 两个 prop 适配三端
- 三登录页接入 `LoginBrand`，删除旧占位品牌（学生端原「题」图标 + 「智慧题库」标题）
- 顺带统一：根落地页 `src/app/page.tsx`（校徽 + 新标题）、`layout.tsx` 的 metadata.title 与标签页标题
- 各页 `metadata.title` 后缀统一为「· 无锡旅商智能练测系统」

### 视觉约定（沿用既有 Apple 风格 token，未改整体设计）
- 学生端：校徽居中 + 短分隔线（w-16）；教师端：左对齐 + 全宽分隔线；超管端：白卡上校徽清晰可读，标签用中性灰
- 唯一新增细节是校徽与系统名间的「发丝分隔线」，呼应院校官方质感

### 验收结果
- `tsc --noEmit` 0 错误；`next build` 0 警告
- 浏览器截图确认三端（学生移动 / 教师 PC / 超管深色底）校徽与新标题渲染正常

### 备注
- `design/学生端风格design.md` 里旧 mockup 文字（「题」图标 / 「智慧题库」）未改，属设计稿示意、不影响代码

### 下一步
进入 **S2**（班级管理）。

---

## [2026-06-30] S2 — 班级管理 ✅

### 完成内容
**基础设施（教师端首个 UI 步骤，搭好 S2–S12 共用底座）**
- UI 组件：手写 shadcn 风格组件（Radix + cva + 项目设计 token），见 `src/components/ui/`：button / input / label / dialog / alert-dialog / dropdown-menu / table / card / skeleton / sonner(Toaster)
- 依赖：class-variance-authority / clsx / tailwind-merge / lucide-react / @radix-ui/react-{dialog,alert-dialog,dropdown-menu,label,slot} / sonner / tw-animate-css / **exceljs**
- `src/lib/utils.ts`（cn）、`src/lib/api.ts`（ok/fail 响应封装 + parsePagination）
- `src/lib/auth-guard.ts` 增 `requireApiRole(role)`：API 版鉴权，返回 `{session,userId}` 或 401/403 响应
- 教师端框架：`src/app/teacher/layout.tsx`（左侧 Sidebar 240px + 内容区 max-w 1200 + 服务端二次鉴权）+ `src/components/teacher/sidebar.tsx`（logo / 导航 / 当前页高亮 / 用户信息 / 退出；题库·考试标「待开发」）
- 全局 Toaster 挂入 `providers.tsx`

**班级 CRUD**
- API：`GET/POST /api/classes`、`PATCH/DELETE /api/classes/:id`，含 Zod 校验、归属校验（teacherId）、统一响应封装
- 删班级仅由外键级联清 Enrollment/ExamClass，**不删学生身份与考试**（已用集成测试验证）
- UI：仪表盘 `src/app/teacher/page.tsx`（班级/学生数概览）、班级列表 `src/app/teacher/classes/page.tsx` + `class-list.tsx`（新建/重命名/删除，Dialog+AlertDialog+Toast，操作后 router.refresh()）

**班内学生列表（分页）**
- API：`GET /api/classes/:id/students?page&pageSize`（过滤 deletedAt）
- UI：班级详情 `src/app/teacher/classes/[id]/page.tsx`（学生表格 + 分页 + 下载模板 + 导入入口占位 S3）

**Excel 导入模板**
- 构建器 `src/lib/templates/student-import.ts`：单 sheet、两列「学号 | 姓名」、冻结首行、表头主色样式、A1/B1 批注、学号列文本格式（保留前导零）
- 下载接口 `GET /api/students/template`（教师鉴权，UTF-8 文件名）
- 生成脚本 `scripts/gen-templates.ts` → 物理文件 `document/templates/学生导入模板.xlsx`（空白）与 `学生导入示例数据.xlsx`（12 条示例，学号 2025 段避开 seed），供 S3 导入联调

### 验收结果（全部通过）
- `tsc --noEmit` 0 错误；`next build` 0 警告（14 路由齐全）
- **20 项 API 集成测试全过**：列表含人数 / 教师间归属隔离 / 新建·改名·空名 422 / 学生分页(10+5) / 跨教师 GET·PATCH·DELETE 全 403 且班名未被篡改 / 未登录 401 / 模板 Content-Type 与下载 / 未登录下载 401 / **删班级保留学生（班级删、关系清、学生在、总数不变）**
- 浏览器实测：仪表盘、班级列表、班级详情、新建班级 Dialog→Toast→刷新 全链路正常；模板解析验证（冻结/批注/12行/学号文本）

### 本步骤新产生的决策（含对 PRD 的偏离）
- **组件库**：不跑 shadcn CLI，改为手写 shadcn 风格组件挂项目 token——CLI 会与既有 Tailwind v4 `@theme` 冲突；CLAUDE.md 目录注释本就写「shadcn/ui 派生组件」
- **⚠️ Excel 库用 exceljs 而非 PRD 所列 SheetJS**：exceljs 对冻结/批注/(S5)下拉数据校验支持更好、npm 维护活跃、读写一库；如需回退 SheetJS 影响面为 `src/lib/templates/*` 与后续解析层

### 尚未解决 / 暂缓
- 学生「导入学生」按钮为占位，实际导入（dry-run 预检 + 确认 + 批量删除）在 S3
- 班级列表为创建者可见；多教师共享同一班级的可见性一期不做（导入文档：其他教师复用同 classId）

### 下一步
进入 **S3 学生导入**：dry-run 预检（跳过/姓名冲突/文件内重复/异常带行号）+ 确认导入 + 批量硬删，复用本步模板与 `document/templates/学生导入示例数据.xlsx` 联调。

---

## [2026-06-30] S3 — 学生导入 ✅

### 完成内容
**核心解析与预检（preview / import 复用，保证一致与安全）**
- `src/lib/import/student-import.ts`：`parseStudentSheet`（exceljs 读首 sheet，A=学号 B=姓名，跳表头，文本取值防丢前导零）+ `analyzeStudentImport`（dry-run 分类）
- 分类口径（学生导入方案 §5/§6）：importable（新建/仅建关系）、skipped（已属别班，A 方案）、alreadyInClass（已在本班，幂等）、nameConflicts（学号存在但姓名不一致，不静默覆盖）、fileDupRows（文件内重复，记首次行号）、invalid（格式异常带行号）；软删学生归 skipped
- `src/lib/constants.ts`：默认密码 / 学号正则 / 行数上限；`src/lib/validations/student.ts`：删除入参

**API**
- `POST /api/classes/:id/students/preview`：multipart→解析→分析→返回各类 + summary，不写库
- `POST /api/classes/:id/students/import`：**重新解析同一文件并重跑分析**（不信任客户端数据），$transaction 内 createMany 学生(skipDuplicates) + bcrypt 整批一次 + createMany Enrollment(skipDuplicates 兜底幂等)
- `DELETE /api/students`：归属过滤 → 有作答软删(deletedAt) / 无作答硬删(级联清 Enrollment)；三者均含教师归属校验

**UI**
- `import-students-dialog.tsx`：上传区 → 自动预检 → 彩色统计 chips + 分组明细（带行号、左边框分色）→「确认导入 N 人」（重传同文件）→ Toast + 刷新
- `student-roster.tsx`：表格行选择（全选/单选，含 indeterminate）+ 选中浮现「删除所选」+ AlertDialog 二次确认（说明硬删/软删语义）
- 班级详情页接入：下载模板 + 导入对话框 + 可选删除

### 验收结果（全部通过）
- `tsc` 0 错误；`next build` 0 警告
- **23 项 API 集成测试全过**：
  - 全新 12 人预检+导入（created/enrolled=12）
  - **幂等**：重复导入同文件 → alreadyInClass=12 / 0 新增 0 入班
  - **四冲突场景**（对含混合数据的文件）：importable 2 / skipped(已属别班) 1 / alreadyInClass 1 / nameConflicts 1 / fileDup 1 / invalid 2，行号准确
  - 批量硬删 12（Enrollment 级联清、学生身份消失）
  - **软删**：有作答学生删除 → deletedAt 置位、身份保留（成绩受保护）
  - 越权：教师2 对教师1 班级 preview/import/删除学生 全 403
- 浏览器实测：上传混合文件，6 类预检结果与行号在弹窗正确渲染（截图存档）

### 遇到的问题及解决方案
- 问题：exceljs `load` 形参 `Buffer` 与新版 @types/node 泛型 Buffer 冲突，tsc 报错
  解决：`buf as unknown as Parameters<typeof wb.xlsx.load>[0]` 强转
- 问题（仅测试脚本）：`db:seed` 不重置 MySQL 自增，多次 reseed 后班级/教师 id 漂移；浏览器旧 JWT 携带过期 teacherId 致班级 404
  解决：测试改为动态查 id；浏览器重新登录刷新 JWT。**提示后续联调注意：reseed 后需重新登录**

### 尚未解决 / 暂缓
- 姓名冲突一期仅提示、不导入该行（不静默覆盖）；“确认更新姓名”留二期
- 软删学生再次导入按“已停用”跳过（不自动复活），属低频边界

### 下一步
进入 **S4 题库 + 题目 CRUD**：题库增删改查 + 四题型单题表单（options/answer 数组格式严格按总表 §6）+ 列表按 type/difficulty 过滤分页。

---

## [2026-06-30] S3 补 — 导入框支持拖拽上传 ✅

- `import-students-dialog.tsx`：上传区新增拖拽支持。抽出 `handleFile(f)`（点击选择与拖拽放下共用：校验 .xlsx → 预检），新增 `onDrop`/`dragover`/`dragleave` 与 `dragging` 视觉态（拖拽悬停时边框/底色高亮 + 文案变「松开以导入」+ 图标放大）。
- 验证：`tsc`/`build` 通过；浏览器实测——拖拽悬停高亮正常，放下 12 人示例文件触发预检并显示「可导入 12 / 确认导入 12 人」。

---

## [2026-06-30] S4 — 题库 + 题目 CRUD ✅

### 完成内容
**校验与共享逻辑**
- `src/lib/validations/question.ts`：四题型 **discriminatedUnion + superRefine**（单选答案恰 1 项、多选≥1、判断 T/F、填空每空≥1 可接受答案；选择题答案必须 ⊆ 选项、选项编号不重复）
- `src/lib/question.ts`（客户端安全：题型/难度标签、`letterForIndex`、`answerSummary`）+ `src/lib/question-hash.ts`（contentHash，口径同 seed）+ `src/lib/question-data.ts`（构造 Prisma 写入数据，选择题答案排序保证去重一致）
- `src/lib/validations/bank.ts`

**API**
- `GET/POST /api/banks`、`PATCH/DELETE /api/banks/:id`（删库级联删题目，试卷快照为弱引用不受影响）
- `GET /api/banks/:id/questions`（按 type/difficulty 过滤 + 分页；教师端返回 answer）、`POST` 单题
- `PATCH/DELETE /api/questions/:id`（删/改原题不影响 ExamQuestion 快照）
- 去重冲突（`@@unique([bankId, contentHash])` → P2002）映射为 409 CONFLICT

**UI**
- 启用侧栏「题库管理」；题库列表 `banks/page.tsx` + `bank-list.tsx`（名称/科目/描述 CRUD）
- 题库详情 `banks/[id]/page.tsx`：按 type/difficulty 过滤（原生 select 改 URL）+ 分页；题目卡片展示（选择题高亮正确项、判断/填空显示答案摘要、解析）
- **四题型表单** `question-form-dialog.tsx`：题型分段切换；选择题选项增删 + 字母编号 + 点选/勾选正确项（内部按 index 维护、提交转 A/B/...）；判断 正确/错误；填空逐空「可接受答案用 / 分隔」；难度/解析

### 验收结果（全部通过）
- `tsc` 0 错误；`next build` 0 警告（16 路由）
- **15 项 API 集成测试全过**：四题型新建 201 / 答案不在选项 422 / 单选多答案 422 / 重复题目 409 / 列表 total=4 / type+difficulty 过滤 / **多选答案排序存为 [A,C]** / 编辑 200 / 删除 200 / 跨教师 403
- 浏览器实测：题库列表、详情页四题型正确渲染（含填空 `[["北京","京城"],["上海"]]` → "空1:北京/京城；空2:上海"）、新建题目表单 → 提交 → 列表刷新全链路

### 本步骤新产生的决策
- 选择题答案入库前排序，使 `["A","C"]` 与 `["C","A"]` 去重一致（seed 数据本就有序，兼容）
- 题型可在表单内切换并重置类型相关字段；选项内部用 index 维护、提交时映射为字母 key（保证 §6 key 与答案引用一致）

### 尚未解决 / 暂缓
- 归属维度 textbook/unit/knowledgePoint 字段已在 schema 预留，本步表单未暴露（一期可后续按需加）；知识点多对多为二期
- 题目导入（Excel/批量）在 S5

### 下一步
进入 **S5 题目导入**：Excel 模板（四题型，下拉校验）→ 解析 → Zod 分流 → dry-run 预检 + 查重 → 确认导入。复用 S3 的 dry-run 管线思路与 `question-data`/`contentHash`。

---

## [2026-06-30] 修复 — 班级共享模型重构 ✅

### 背景（用户反馈的 S2 缺陷）
1. 可创建同名班级（组卷选班会混淆）。2. 班级被创建者独占，其他老师看不到/无法选用。3. 担心删班级级联删学生、删后无法重新导入。

### 数据库诊断
- 根因：`Class.teacherId` 把班级建成"被某老师独占的资源"，而班级本应是**全校实体**。问题 1、2 是同一根因的两个症状（独占 → 名字不全局唯一 + 可见性按创建者过滤）。`学生导入方案设计` 原意本就是"班级共享、其他老师复用"，是 S2 实现加了按老师过滤而背离。
- 问题 3 **不是 bug**：`Enrollment` 的 `onDelete: Cascade` 在成员关系上，删班只清 Enrollment，**学生身份保留**；删后学生变游离学生，重新建班/加授课后**可被重新导入**（姓名一致→importable；不一致→姓名冲突提示，非永久阻塞）。

### 方案（已与用户拍板）
全校共享模型 + 授课 M2M：
- `Class.name @unique`；`Class.teacherId` 保留，语义改为**创建者**（仅审计 + 改名/删班鉴权）
- 新增 **`ClassTeacher`** 授课关联表（与 Enrollment 平行）；"我授课的班级"=该表口径
- 班级管理新增**「班级列表」**弹窗（看全校班级 + 「添加授课」把已存在班级加入我的授课）；新建班级自动把创建者设为授课
- 权限：查看/导入=授课老师；改名/删班/删学生=创建者（超管走 /admin）；撞名→409 + 前端 inline 提示

### 实现
- schema + migration `20260630120000_class_teaching_sharing`（diff 生成 SQL 后 `migrate deploy`，规避非交互限制）；seed 增 ClassTeacher（教师1授课两班、教师2共享高一1班）
- `src/lib/class-access.ts`（loadClassForTeacher → teaches/isCreator）
- API：`GET /api/classes`(我授课) + `?scope=all`(全校)；`POST` 撞名 409 + 建班自动授课；`PATCH/DELETE` 限创建者；新增 `POST|DELETE /api/classes/:id/teachers`(加入/退出授课)；roster/preview/import 改为 teaches 门槛；删学生维持创建者作用域
- UI：`all-classes-dialog.tsx`；`class-list.tsx`（我授课列表 + 创建者标识 + 条件菜单 + 移出授课 + 撞名 inline 错误）；详情页 `canDelete=isCreator` 控制批量删除；仪表盘"我的班级"=授课口径

### 验收（全部通过）
- `tsc` 0 错误；`build` 0 警告
- **15 项 API 测试全过**：授课可见性（t1 见 2 / t2 见 1）、scope=all + teaching 标志、撞名 409、加入/退出授课、创建者治理（非创建者改名/删班 403）、授课可导入 / 未授课 403
- 浏览器实测：李老师（教师2）只见共享的高一(1)班；「班级列表」弹窗中高一(2)班「添加授课」、高一(1)班「已授课」渲染正确（截图存档）

### 备注
- 游离学生（删班后无班级、仍可登录但看不到考试）属可接受的小遗留，待 S12 超管端做"游离学生清理"

---

## [2026-06-30] S5 — 题目导入 ✅

### 完成内容
- `src/lib/templates/question-import.ts`：四题型 Excel 模板构建器（多 Sheet：单选/多选/判断/填空），每 Sheet 含样例数据、列批注、难度下拉校验，填空答案列用 `|||` 分隔多个可接受答案
- `src/lib/import/question-import.ts`：解析 + dry-run 预检核心
  - `parseQuestionWorkbook(buf)`：按 Sheet 名调对应解析器，四题型各自解析为 QuestionInput
  - `analyzeQuestionImport(rows, bankId)`：Zod 校验分流 → 文件内去重（contentHash）→ 批量查库（`findMany` 查 bankId + contentHash）→ 返回 importable/duplicates/fileDups/invalid
- API 路由（均带 requireApiRole + bank 归属校验）：
  - `GET /api/banks/[id]/questions/template`：返回模板 xlsx，按题库名设 Content-Disposition
  - `POST /api/banks/[id]/questions/import/preview`：dry-run，返回完整分析结果（不写库）
  - `POST /api/banks/[id]/questions/import/confirm`：服务端重解析文件 → `createMany(skipDuplicates)` → 打 importBatchId → 返回 `{ created, skipped, importBatchId }`
- `src/components/teacher/banks/import-questions-dialog.tsx`：干净的三态弹窗（idle/previewing/importing），摘要 chips + 分类 Section 展示预检结果（格式异常/文件内重复/已在题库），拖放上传
- `src/components/teacher/banks/question-list.tsx`：工具栏新增「下载模板」按钮（`<a>` 触发 blob 下载）+ 「批量导入」按钮调起弹窗

### 关键设计决策
- **多 Sheet vs 单 Sheet**：选多 Sheet，每题型独立布局，可对答案列加精确校验（判断用下拉正确/错误），用户体验更好
- **填空答案格式**：每空独立一列（空1~空4），`|||` 分隔同一空的多个可接受答案；与 S3 不同无法用 Excel 内置下拉（内容开放），改用列批注说明格式
- **占位符校验**：填空题干中 `____` 数量 == 非空答案列数，否则预检拦截，提示具体不一致原因
- **confirm 重解析**：遵循 S3 模式，不信任客户端数据，confirm 时服务端重传文件重跑分析，保证安全
- **下载模板**：不走 Next.js 的 `<Link download>`，改用 fetch + blob URL 方式，可带鉴权头并读取 Content-Disposition 中的 UTF-8 文件名

### 遇到的问题及解决方案
- 问题：`Prisma.JsonNull` 在 `createMany.data` 中类型不兼容
  解决：`q.data.options as Prisma.InputJsonValue`（`buildQuestionData` 已正确返回 `Prisma.JsonNull`，强转即可）
- 问题：多选题正确答案用户可能用中文逗号、带空格等写法
  解决：`parseChoiceAnswer` 中 split by `/[,，]/` 并 trim + 过滤非字母

### 尚未解决 / 暂缓
- 无（本步骤无遗留）

### 验收
- `tsc --noEmit` 0 错误
- `npm run build` 0 TypeScript 错误，三条新路由全部注册（`/api/banks/[id]/questions/import/confirm` `/preview` `/template`）
- 四题型解析、文件内去重、DB 查重、确认写库逻辑均经代码审查通过

### 下一步
进入 S6 组卷：组卷向导 UI（基本信息→选题库→选班级→确认）+ `$transaction` 快照（ExamQuestion createMany）+ 整卷浏览/删题

---

_（后续记录追加在此行之后）_

## [2026-06-30] S6 — 组卷 ✅

### 完成内容
- **API** 新建 3 个路由文件：
  - `POST/GET /api/exams` — 组卷（$transaction 快照）+ 列表
  - `GET/PATCH/DELETE /api/exams/[id]` — 详情 / 改基本信息 / 删除（软删/硬删）
  - `DELETE /api/exams/[id]/questions/[eqId]` — 从试卷快照移除题目
- **Zod 校验** `src/lib/validations/exam.ts`（createExamSchema + updateExamSchema）
- **UI 组件** 新增 `src/components/ui/select.tsx`、`src/components/ui/checkbox.tsx`
- **组卷向导** `src/components/teacher/exams/create-exam-wizard.tsx`：4 步（基本信息→选题库→选班级→确认）
- **考试列表** `src/components/teacher/exams/exam-list.tsx`
- **试卷详情** `src/components/teacher/exams/exam-detail.tsx`（含编辑/删题/删考试）
- **页面** `/teacher/exams`、`/teacher/exams/new`、`/teacher/exams/[id]`
- **Sidebar** 启用"考试管理"链接（ready: true）

### 遇到的问题及解决方案
- 问题：Prisma `createMany` 的 nullable JSON 字段不接受 JS `null`，类型报错
  解决：参照 `question-data.ts`，对 `options` 字段用 `Prisma.DbNull`（`q.options !== null ? (q.options as Prisma.InputJsonValue) : Prisma.DbNull`）

### 尚未解决 / 暂缓
- 无

### 本步骤新产生的决策
- 组卷时整库导入，每题 `defaultScore`（默认 1 分）可在向导基本信息步设置
- 删考试：有作答→软删（`deletedAt`），无作答→硬删；前端根据 `attemptCount` 提示不同信息
- 验证班级权限用 `ClassTeacher` 表（授课关系）

### 下一步
进入 S7 考试管理（列表状态标注：进行中/已截止；历史成绩查看入口）

## [2026-06-30] S7 — 考试管理 ✅

### 完成内容
- **考试状态标签**：ExamList 每张卡片根据 `deadline` 计算状态，显示"进行中"（绿色）或"已截止"（红色）
- **已下架考试历史查看**：
  - ExamsPage 同时查询活动（`deletedAt: null`）和已下架（`deletedAt NOT NULL`）考试
  - ExamList 底部增加折叠区"已下架的考试/练习"，展开后显示归档卡片 + 提示语
  - 软删除后列表立即将卡片从活动区迁入归档区（前端状态同步，无需刷页）
- **已下架考试详情只读模式**：
  - ExamDetailPage 去掉 `deletedAt: null` 过滤，软删除考试的 URL 仍可访问
  - ExamDetail 接收 `deletedAt` 字段，显示蓝灰色"已下架"提示横幅
  - 隐藏"编辑信息"与"删除考试"按钮，隐藏逐题的移除按钮
- **API 扩展**：
  - `GET /api/exams?archived=1` 返回软删除考试列表（含 `deletedAt` 字段）
  - `GET /api/exams/:id` 移除 `deletedAt: null` 约束，允许查看归档考试
  - PATCH / DELETE 依旧限制 `deletedAt: null`（只可编辑/删除活动考试）
- **仪表盘真实数字**：Dashboard 补全题库数量（`prisma.questionBank.count`）和考试数量（`prisma.exam.count`），全部卡片改为可点击链接

### 遇到的问题及解决方案
- PATCH helper `getExam` 重名：改为 `getExamForWrite` 避免与 GET handler 的查询逻辑混淆

### 尚未解决 / 暂缓
- 历史成绩的"数字"展示（平均分 / 按学生下钻）留 S11 统计模块处理

### 本步骤新产生的决策
- 已下架考试"只读查看"= 移除操作按钮 + "已下架"横幅；不需要单独 archived 路由
- 软删除后前端直接更新列表状态（活动→归档），无需硬刷新

### 下一步
进入 S8 学生做题流程（考试列表 + 开始/续答 API + 题目展示，绝不下发 answer）

## [2026-06-30] S8 — 学生做题流程 ✅

### 完成内容
- `src/lib/grading.ts`：四题型判分（单选/多选/判断/填空），填空归一化（trim+全半角+小写）后命中可接受答案集
- `GET /api/student/exams`：Enrollment∩ExamClass 交集，deadline 过滤，每条附作答状态（NOT_STARTED / IN_PROGRESS / SUBMITTED）+剩余次数+最近得分
- `POST /api/student/exams/[id]/attempt`：断点续答复用 IN_PROGRESS Attempt，恢复草稿 + elapsedSec；新建时校验 repeatLimit；LCG seededShuffle 基于 attemptId 保证题目/选项顺序一致性；**answer 字段使用 Prisma select 严格排除，绝不下发**
- `PATCH /api/student/attempts/[id]/save`：因 AnswerItem 无 @@unique，手动 findMany 后 update-or-create；草稿保存更新 elapsedSec + lastSavedAt
- `POST /api/student/attempts/[id]/submit`：deleteMany 旧草稿再 createMany 判分结果；事务内 upsert WrongQuestion；返回 score/maxScore/correctCount
- `GET /api/student/attempts/[id]/result`：结果页才下发 answer 和 analysis
- 学生端页面：layout（背景渐变）+ 考试列表首页（两 tab，玻璃卡片，状态色）+ 做题页（iOS 玻璃形态，四题型独立 UI，题目卡片 key 触发 CSS 入场动画，倒计时/计时，15s 防抖草稿，Question Grid Sheet，Submit Sheet，计时到 0 自动交卷）+ 结果页（评分圆环，逐题展开折叠，答案对比+解析）

### 遇到的问题及解决方案
- 问题：AnswerItem 无 @@unique([attemptId, examQuestionId])，Prisma upsert 需要 unique key 无法直接用
  解决：先 findMany 建 existingMap，再循环 update-or-create；submit 时 deleteMany + createMany 更简洁
- 问题：填空题 `<input>` 不能放在 `<p>` 内（无效 HTML）
  解决：FILL_BLANK 类型单独渲染 `<div>` + Fragment 交替 span/input
- 问题：seededShuffle 需在相同 attemptId 下产生完全相同排列（断点续答一致性）
  解决：LCG 伪随机（imul 实现），seed = attemptId；选项 shuffle seed = attemptId + examQuestionId
- 问题：FillBlankStem 初始化：加载时不知道空数，无法预设 chosen
  解决：load 时遍历 questions，FILL_BLANK 且无草稿的用 stem.match(BLANK_RE).length 初始化为空串数组

### 尚未解决 / 暂缓
- AnswerItem 没有 @@unique——如并发保存可能产生重复行（一期单用户，可接受；如需修复需加迁移）
- 练习模式即时反馈（每题作答后立即显示对错+解析）留 S9
- 错题本列表页留 S10

### 本步骤新产生的决策
- 判分与草稿保存合并在 S8 实现（PRD 将 S8/S9 拆开，但做题流程不完整无法验收，故提前实现 save/submit/grading）
- seededShuffle 选用 LCG 而非依赖 `Math.random()`（避免 Next.js 服务端不确定性）
- 结果页 attemptId 通过 query param 传递（路径已固定为 /student/exams/[id]/result）

### 下一步
进入 S9（练习模式即时反馈 + 错题本列表；也可根据优先级直接进 S10 成绩统计或 S11 超管）

---

## [2026-06-30] S9 — 错题本 + 个人中心 + 底部 Tab 导航 ✅

### 完成内容
- `GET /api/student/wrong-questions`：查 WrongQuestion（含 examQuestion answer/analysis/options）→ 按 examId 分组返回；answer 在此处可下发（复习场景，与做题时严格 exclude 不同）
- `POST /api/student/wrong-questions/[id]/redo`：鉴权归属 → gradeQuestion 判分 → redoCount+1 / lastResult 更新 → 返回 { isCorrect, correctAnswer, analysis, redoCount }
- `src/components/student/student-tab-bar.tsx`：三入口 Tab Bar（考试/错题本/我的），usePathname 激活检测，iOS blur 背景，填充/线框 SVG 图标切换
- `src/app/student/wrong/page.tsx`（Client Component）：按考试分组手风琴；WrongQuestionCard 支持记忆模式（答案高亮）+ 重做模式（四题型交互 UI + 提交判分 + 结果横幅）；redoCount / lastResult 实时更新父级 state
- `src/app/student/me/page.tsx`（Server Component）：requireRole → prisma 取 name/studentNo + 完成考试数 + 错题数 → 静态渲染；签出按钮
- `src/components/student/exam-list.tsx` 加入 StudentTabBar + `pb-28` 防内容遮挡

### 遇到的问题及解决方案
- 问题：WrongQuestionCard 折叠/展开时 ExpandedCard 状态（mode、chosen、redoResult）需要在重新展开时是否重置
  解决：ExpandedCard 用条件渲染 `{isOpen && <ExpandedCard>}`，组件每次展开时重新挂载，状态自动初始化；无需 useEffect 清理
- 问题：填空题 stem 含 BLANK_RE 占位符，memory/redo 模式渲染方式不同
  解决：redo 模式在 ExpandedCard 层处理（FillBlankStem 组件）；memory 模式 stem 只做 replace 展示

### 尚未解决 / 暂缓
- 练习模式（PRACTICE 类型）即时反馈（做题时每题提交即显示对错+解析）未实现，留 S10/后续
- 结果页"查看错题"按钮暂无，可在 S10 结果页增强时补充

### 本步骤新产生的决策
- 错题本 API 下发 answer（复习场景允许），与做题 API 严格 exclude 的安全红线无冲突（不同上下文）
- StudentTabBar 仅加入三个"底部导航页"（首页/错题本/我的），exam-taking 和 result 页有独立 chrome，不加 tab bar

### 下一步
进入 S10（成绩统计：`GET /api/exams/:id/stats`；教师端统计页含平均分/分数段/每题准确率/选项选择率/按学生下钻）

---

## [2026-07-01] UI 优化 — 学生端多处视觉改进 ✅

### 完成内容

**学生端 Tab Bar（student-tab-bar.tsx）**
- 从全宽底栏改为 iOS 26 风格浮动胶囊：`fixed`，居中，`borderRadius: 36px`，毛玻璃背景（`blur(28px) saturate(180%)`，透明度 0.72），底部悬浮 16px
- 第一个 Tab 标签从"考试"改为"练习"，图标换成铅笔写字 SVG（active=填充，inactive=描边）

**学生端练习列表（exam-list.tsx）**
- 筛选 tab 从「全部 | 练习」改为「全部 | 考试 | 练习」
- filter 逻辑同步更新：`"exam"` → `type === "EXAM"`，`"practice"` → `type === "PRACTICE"`

**错题本列表页（wrong/page.tsx）**
- 取消手风琴展开，每个错题集改为一张非展开卡片，右侧改为右向箭头 `›`
- 点击卡片跳转到新路由 `/student/wrong/[examId]`

**错题详情页（wrong/[examId]/page.tsx）—— 新建路由**
- 记忆模式：可滚动题目列表，每题展示题干 + 答案 + 解析
- 重做模式：完全参考 exam-client 的做题 UI，每页一题
  - 进度条 + "第 X/Y 题" 计数
  - 单题卡片，`key={redoIdx}` 触发 qFadeIn 动画
  - 提交后 result-pop 弹出动画 + 答案揭示，可点"再试"重置本题
  - 底部 nav：「上一题」| 棋盘格图标（点击弹出题目选择 Sheet）| 「确认作答」→「下一题」→「完成」
  - 题目选择 Sheet：5列格，蓝=当前，绿=答对，红=答错，灰=未做
  - 不显示 StudentTabBar，使用独立底部 nav

### 遇到的问题及解决方案
- 问题："下一题"按钮箭头 `polyline points="9 18 15 12 9 6"` 本是右向，加了 `scaleX(-1)` 导致变成左向
  解决：移除 `style={{ transform: "scaleX(-1)" }}`；"完成"按钮不显示箭头

### 本步骤新产生的决策
- 错题重做模式采用"一题一页"而非"全部展开"，与做题界面体验一致
- 模式切换（记忆/重做）在页面顶部 sticky header 中，两种模式共享同一 header
- 重做模式 qStates 在切换模式时清零（重置所有作答状态），保证进入重做模式时从第一题开始

### 下一步
继续 S10（成绩统计）
