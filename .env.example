# 环境变量模板 —— 复制为 .env 并填入真实值
# 本地开发：指向本机 MySQL；生产：指向 docker-compose 中的 db 服务
DATABASE_URL="mysql://用户名:密码@localhost:3306/exam_system"

# NextAuth 密钥，生产环境用 openssl rand -base64 32 生成
NEXTAUTH_SECRET="生成一个随机密钥"
NEXTAUTH_URL="http://localhost:3000"
